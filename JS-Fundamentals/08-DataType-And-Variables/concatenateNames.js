function concatenateNames(first, second, sumbol) {
let result = first+sumbol+second
console.log(result);
}

concatenateNames('John','Smith','->');
concatenateNames('Jan',
'White',
'<->'
);
concatenateNames('Linda',
'Terry',
'=>'
);

